/** Where the join URL was created / what kind of link `meetingUrl` points to. */
export enum MeetingLinkProvider {
  GOOGLE_MEET = 'google_meet',
  MICROSOFT_TEAMS = 'microsoft_teams',
  ZOOM = 'zoom',
  OTHER = 'other',
}
